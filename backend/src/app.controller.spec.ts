// Endpoints manually tested using Postman and the UI
// Verified registration, login, authentication, and CRUD operations
// Wasn't aware of testing with app.controller.spec.ts until after completing manual checks
